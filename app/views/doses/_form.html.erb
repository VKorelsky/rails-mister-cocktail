<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, label: "How do you mix it in?"%>
  <%= f.input :ingredient_id, collection: Ingredient.all%>
  <%= f.button :submit, "Submit", class: "btn btn-primary" %>
<% end %>
